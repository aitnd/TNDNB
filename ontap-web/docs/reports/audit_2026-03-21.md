# Audit Report - 2026-03-21

## Summary
- 🔴 Critical Issues: 2
- 🟡 Warnings: 2
- 🟢 Suggestions: 2

## 🔴 Critical Issues (Phải sửa ngay)
1. **Lộ Private Key Admin của Firebase**
   - File: `/thi-tnd-firebase-adminsdk-fbsvc-cef6c2f079.json`
   - Nguy hiểm: File này chứa chìa khóa vạn năng (Private Key) có thể điều khiển toàn bộ dự án Firebase của bạn. Nếu lộ ra ngoài, hacker có thể xóa sạch dữ liệu hoặc mạo danh admin.
   - Cách sửa: Xóa file này khỏi thư mục dự án ngay lập tức. Nếu cần dùng cho Server, hãy sử dụng biến môi trường (Environment Variables).

2. **File nhạy cảm không được bỏ qua bởi Git**
   - File: `.gitignore` thiếu định dạng để chặn file JSON của Firebase.
   - Nguy hiểm: Bạn có thể vô tình đẩy (push) file chứa khóa bí mật lên GitHub/GitLab.
   - Cách sửa: Thêm `*.json` (nếu không cần thiết) hoặc chính xác tên file service account vào `.gitignore`.

## 🟡 Warnings (Nên sửa)
1. **File App.tsx quá "cồng kềnh"**
   - File: `App.tsx` (~1000 dòng)
   - Triệu chứng: File này đang làm quá nhiều việc: quản lý Route, xử lý Logic, quản lý State toàn cục.
   - Hậu quả: Rất khó để tìm lỗi hoặc thêm tính năng mới mà không làm hỏng các phần khác.
   - Cách sửa: Tách các Route thành các Module riêng và đưa logic xử lý State vào các Hook hoặc Store chuyên biệt.

2. **Kích thước file sau khi nén quá lớn**
   - Triệu chứng: File `index-BWGl3fyB.js` lên tới 2.6MB (750KB gzip).
   - Hậu quả: Người dùng vào trang web sẽ phải chờ lâu để tải xong code, làm giảm trải nghiệm và SEO.
   - Cách sửa: Sử dụng kỹ thuật `Code Splitting` (React.lazy) để chỉ tải những trang cần thiết.

## 🟢 Suggestions (Tùy chọn)
1. **Tối ưu hóa Vite Config**
   - File: `vite.config.ts`
   - Hướng dẫn: Thêm cấu hình `manualChunks` để tách các thư viện lớn như `firebase`, `recharts` ra khỏi file code chính.

2. **Kiểm tra lại việc dùng firebase-admin ở Frontend**
   - File: `package.json`
   - Triệu chứng: Có cả `firebase` và `firebase-admin`. 
   - Hướng dẫn: `firebase-admin` thường chỉ dùng cho Server. Nếu web app thuần client thì nên gỡ bỏ để giảm dung lượng.

## Next Steps
Tôi đã chuẩn bị sẵn các phác đồ điều trị. Bạn có muốn tôi bắt đầu xử lý các lỗi NGUY HIỂM trước không?
