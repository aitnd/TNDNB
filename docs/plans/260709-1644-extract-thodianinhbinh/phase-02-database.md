# Phase 02: Database Schema (Firebase)
Created: 2026-07-09 16:44

## Tasks
- [ ] The user needs to create a new Firebase project (or we guide the user to do so).
- [ ] Obtain the new Firebase configuration (apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId).
- [ ] Create a `.env.local` file in `ThoDiaNinhBinh`.
- [ ] Update `src/firebase.ts` or corresponding files to use the new environment variables instead of hardcoded configs (if any).
- [ ] Verify database connection locally.
